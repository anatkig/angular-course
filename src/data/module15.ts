import type { Module } from '../types';

export const module15: Module = {
  id: 'mod-15',
  title: 'Performance Optimization',
  description: 'Optimize Angular apps with OnPush, lazy loading, trackBy, deferrable views, and profiling.',
  topics: [
    { id: 'mod15-t1', title: 'Change Detection & OnPush', explanation: `## Change Detection Strategies\n\nAngular checks the component tree for changes to update the DOM.\n\n### Default Strategy\n\nChecks the entire component tree on every event, timer, or HTTP response.\n\n### OnPush Strategy\n\n\`\`\`typescript\n@Component({\n  changeDetection: ChangeDetectionStrategy.OnPush,\n  template: '...',\n})\nexport class MyComponent {\n  @Input() data!: Data;\n}\n\`\`\`\n\nWith OnPush, Angular only checks when:\n- An @Input reference changes\n- An event originates from the component\n- An Observable emits (via async pipe)\n- Manually triggered via ChangeDetectorRef\n\n### Deferrable Views (Angular 17+)\n\n\`\`\`html\n@defer (on viewport) {\n  <app-heavy-chart />\n} @placeholder {\n  <div>Chart loading...</div>\n} @loading (minimum 500ms) {\n  <app-spinner />\n}\n\`\`\`\n\n### Triggers\n- \`on viewport\` — when element enters viewport\n- \`on idle\` — when browser is idle\n- \`on interaction\` — on user interaction\n- \`on hover\` — on mouse hover\n- \`on timer(5s)\` — after delay\n- \`when condition\` — when expression is true`, tasks: [{ description: 'Convert a component to OnPush change detection and use @defer for a heavy child component.', starterCode: `// TODO: Apply OnPush strategy and @defer`, solution: `import { Component, ChangeDetectionStrategy, Input } from '@angular/core';\n\n@Component({\n  selector: 'app-dashboard',\n  standalone: true,\n  changeDetection: ChangeDetectionStrategy.OnPush,\n  template: \\\`\n    <h1>{{ title }}</h1>\n    @defer (on viewport) {\n      <app-analytics-chart [data]="chartData" />\n    } @placeholder {\n      <div class="placeholder">📊 Chart will load when visible</div>\n    } @loading (minimum 300ms) {\n      <div class="loading">Loading chart...</div>\n    }\n  \\\`,\n})\nexport class DashboardComponent {\n  @Input() title = 'Dashboard';\n  @Input() chartData: number[] = [];\n}`, hints: ['OnPush skips checks unless inputs change by reference', 'Use immutable data patterns with OnPush', '@defer delays loading until triggered'] }] },
  ],
  test: [
    { id: 'mod15-q1', question: 'What does ChangeDetectionStrategy.OnPush do?', options: ['Disables change detection', 'Only checks when inputs change by reference, events fire, or async pipe emits', 'Pushes changes to the server', 'Runs change detection more frequently'], correctAnswer: 1, explanation: 'OnPush optimizes change detection by skipping the component unless its input references change, an event fires from it, or an Observable emits via async pipe.' },
    { id: 'mod15-q2', question: 'What is @defer used for?', options: ['Deferring errors', 'Lazy loading parts of a template with configurable triggers', 'Delaying HTTP requests', 'Postponing navigation'], correctAnswer: 1, explanation: '@defer lazily loads template blocks based on triggers (viewport, idle, interaction, timer), reducing initial bundle size and improving performance.' },
    { id: 'mod15-q3', question: 'Why is trackBy important in @for / *ngFor?', options: ['For animation', 'It helps Angular identify unchanged items to minimize DOM manipulation', 'For sorting', 'For filtering'], correctAnswer: 1, explanation: 'trackBy (or track expression in @for) provides a unique identity for each item, so Angular can reuse existing DOM elements instead of recreating them.' },
    { id: 'mod15-q4', question: 'What does the async pipe help with performance?', options: ['It makes code asynchronous', 'It auto-subscribes/unsubscribes and works with OnPush change detection', 'It caches responses', 'It parallelizes rendering'], correctAnswer: 1, explanation: 'AsyncPipe automatically subscribes, triggers change detection on emission, and unsubscribes on destroy — ideal with OnPush.' },
    { id: 'mod15-q5', question: 'What tool helps profile Angular performance?', options: ['Chrome DevTools Performance tab', 'Angular DevTools browser extension', 'Both A and B', 'ng test'], correctAnswer: 2, explanation: 'Both Chrome DevTools Performance tab and the Angular DevTools browser extension help profile change detection cycles and component rendering.' },
  ],
};
